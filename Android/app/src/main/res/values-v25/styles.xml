<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Button" parent="Widget.Design.FloatingActionButton">
        <!-- reference our hint & error styles -->
        <item name="hintTextAppearance">@style/HintText</item>
        <item name="errorTextAppearance">@style/ErrorText</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorHint">@color/white</item>
        <item name="colorControlNormal">@color/white</item>
        <item name="colorControlActivated">@color/white</item>
        <item name="colorControlHighlight">@color/white</item>
        <item name="background">@color/transparent</item>
        <item name="android:fillColor">@color/transparent</item>

        <item name="android:colorPrimary">@color/transparent</item>
        <item name="android:colorSecondary">@color/transparent</item>
        <item name="android:colorPrimaryDark">@color/transparent</item>
        <item name="android:colorAccent">@color/transparent</item>
    </style>
</resources>